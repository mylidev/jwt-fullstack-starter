/**
 * validations/index.ts
 */

import account from './account.validations';
import workspace from './workspace.validations';
import object from './object.validations';

export const accountValidations = account;
export const workspaceValidations = workspace;
export const objectValidations = object;
